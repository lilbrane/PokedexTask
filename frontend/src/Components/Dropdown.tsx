import React, { useState } from 'react'

const Dropdown = () => {
    const [isOpen, setIsOpen] = useState(false);
    const [selectedPokemon, isSelectedPokemon] = useState("");

  return (
    <div>
        
    </div>
  )
}

export default Dropdown